image: python:3.8-slim

pages:
  stage: deploy
  script:
    - pip install -e .
    - pip install -U sphinx sphinx-rtd-theme
    - cd docs/
    - sphinx-build -b html . ../public
  artifacts:
    paths:
    - public
  only:
    refs:
      - docs
    changes:
      - .gitlab-ci.yml
      - docs/*
